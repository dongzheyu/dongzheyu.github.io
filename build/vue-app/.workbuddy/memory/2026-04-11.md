# 2026-04-11 工作日志

## 深度审计与修复

完成 Vue 项目全面美学/性能/用户体验审计，发现并修复以下问题：

### 已修复（P0-P1）
1. **ease-in-out 违规**：main.css 第1275行 floating-element 动画，替换为 `cubic-bezier(0.25, 0.1, 0.25, 1)`
2. **紫色/靛蓝色违规**（4处）：
   - bipolar 按钮: #7209b7 → #c9184a（深玫红）
   - ocd 按钮: #3a0ca3 → #d00000（深红）
   - adhd 按钮: #f72585→#7209b7 → #ffba08→#f48c06（琥珀橙）
   - bpd 按钮: #9b5de5 → #dc2f02（深橙）
   - EmergencyView mental-card: #7209b7 → #c9184a
3. **未定义 CSS 变量**（6处）：
   - --color-text-primary → var(--color-heading)
   - --color-bg-secondary → var(--color-bg-soft)
   - --color-border-light → var(--color-border)
   - --color-surface → var(--color-bg-card)
4. **EmergencyView ease 违规**（4处）：全部替换为自定义贝塞尔曲线
5. **频率按钮/是否按钮 ease**：替换为 --ease-sharp
6. **BlogView 颜色违规**：C#文章 #9B59B6→#dc2f02，XAML #3498DB→#52b788
7. **ADHD 深色主题 fallback**：加入 .btn-test-adhd 到深色主题覆盖列表

## 悬浮图标重叠修复

**问题**：HomeView 右侧装饰区的 4 个 `.floating-element` 全部堆在 `(0,0)`，因为 main.css 只声明了 `position: absolute` 但没给 `nth-child` 设坐标。

**修复（main.css）**：
- 为 4 个子元素添加 `nth-child(1~4)` 各自的 `top`/`left` 错落坐标（8%/55%, 38%/12%, 62%/62%, 18%/22%）
- 添加负值 `animation-delay` 让它们异相浮动（-0s, -1.8s, -3.2s, -0.9s）
- 修正 `animation` 属性使用 `var(--duration, 6s)` 接入模板中设置的 CSS 变量

---

### 未修复（P2/P3，需较大工作量或为可选改进）
- 导航栏 border-image 改 ::after 伪元素
- 内联样式减少（39处 :style= 绑定）
- AboutView project-card 样式重复
- HomeView/TestsView features/tests 数组 Object.freeze
- App.vue addRipple 性能优化（rAF 节流）
- Features 区域 Bento Grid 不对称布局改造
