# 2026-04-10 日记

## 第一阶段重构完成
1. **合并 animations.css 到 base.css** — GPU加速(translate3d)，弹性曲线，prefers-reduced-motion 支持，删除 animations.css
2. **优化 useAnimations.ts** — 新增 useScrollAnimation（操作 classList）+ useReducedMotion
3. **重构 HomeView.vue** — CSS Grid 1.2fr/0.8fr 非对称 Hero + Bootstrap row 错位卡片 + CTA Flex

## 第二阶段重构完成
4. **字体系统优化** — html 基准 15→16px，body 14→15px，行高 1.65→1.7；2K(17px)/4K(18px)大屏适配；h1-h6 line-height:1.25
5. **测试按钮交互增强** — freq-btn/yesno-btn 3D按压感（底部阴影+hover上浮+active下压+selected光晕）
6. **AboutView 留白节奏** — 从全部统一 3rem 改为差异化：密集区 4-5rem、轻量区 3rem、收尾区 5-6rem

## 第三阶段 — Xe 核显性能优化（针对集成显卡优化）
7. **will-change 清理** — 移除 card-hover/btn-animate/img-animate 上的永久 will-change，HomeView animate-on-scroll 的 will-change 也移除；仅保留 body::before 的 will-change:auto
8. **移除持续背景动画** — heroGlow(25s)、bgPulse(18-20s) 全部改为静态渐变；About/Blog/Projects Hero 的 bgPulse 动画移除；减少渐变层数（3层→2层，降低 opacity）
9. **简化 backdrop-filter** — 导航栏 navbar-blur 从 `saturate(180%) blur(24px)` 简化为 `blur(20px)`；glass-blur 同样简化
10. **噪点纹理轻量化** — SVG feTurbulence 从 numOctaves=4/baseFrequency=0.9 降到 octaves=2/freq=0.65；opacity 从 0.04→0.03；移除 mix-blend-mode:overlay
11. **悬停阴影简化** — 所有卡片/按钮 hover 时从双重 shadow(`shadow-xl + shadow-warm`) 改为单层 `shadow-lg`；translateY 上浮量从 -8/-6/-3px 减到 -4/-2/-1px
12. **测试按钮轻量化** — freq-btn/yesno-btn 移除 3D 按压阴影(box-shadow 底部阴影)和光晕效果(0 0 20px)，改为 scale(1.02/0.98) 缩放反馈
13. **移除昂贵的 CSS 属性** — flipIn 关键帧(perspective+rotateY) 移除；tilt-card preserve-3d/translateZ 移除；filter:brightness(1.1) 全局按钮滤镜移除；floating-element drop-shadow + transition 移除；test-hero::before 噪点纹理移除
14. **prefers-reduced-motion 精简** — 通配符一次性禁用所有动画/过渡/滚动行为，移除逐元素列表匹配

## Bug 修复
- HomeView 卡片不显示：useScrollAnimation 必须操作 DOM classList 而非仅更新 ref
- Features 样式丢失：必须保持 Bootstrap row 结构以兼容 main.css
