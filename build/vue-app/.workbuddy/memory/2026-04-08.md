# 2026-04-08

## 导航栏更新
- 将导航栏中的"测试"改为"心理评估"
- 移除下拉菜单，改为直接跳转到 `/tests` 页面
- 更新了所有测试页面的返回链接文本，从"返回测试列表"改为"返回评估列表"
- 更新了 TestsView.vue 页面标题，从"心理测试"改为"心理评估"
- 更新了测试卡片中的"开始测试"按钮文本，改为"开始评估"

## 修复编码问题
- 修复了 OcdView.vue、EatingView.vue、SubstanceView.vue 三个文件的编码问题
- 重新创建了这三个文件，使用正确的 UTF-8 编码
- 确保所有测试页面都有正确的标题和返回链接

## 验证结果
- 运行 linter 检查，所有文件无语法错误
- 导航栏现在显示"心理评估"，点击直接跳转到测试汇总页面
- 所有16个测试页面都已完成并可以正常访问

## 求救页面创建
- 创建了 EmergencyView.vue 求救页面
- 页面包含报警电话、急救电话、火警电话、心理援助热线等紧急联系电话
- 包含常用自救方法：心肺复苏、外伤止血、火灾逃生、心理危机应对
- 添加了路由配置，路径为 `/emergency`
- 在导航栏添加了"紧急求助"链接
- 页面设计符合项目主题，有良好的视觉层次和交互效果

## 样式重构和优化
- 清理了Vue文件中的内联样式属性，将它们移动到CSS文件中
- 为测试页面的 `.freq-instruction` 类添加了 `padding-left: 5%` 样式，移除了重复的内联样式
- 添加了通用的 `.text-left` 类，替换了重复的 `text-align: left !important` 内联样式
- 为HomeView页面添加了 `.hero-content` 类，替换了 `padding-left: 4%` 内联样式
- 添加了缺失的 `animate-delay-150` 和 `animate-delay-450` 类，用于替换动画延迟内联样式
- 将 EmergencyView.vue 中的大量 `<style>` 块样式移动到 main.css 文件中，使样式统一管理
- 项目构建成功，验证了所有修改的正确性

## 深色主题文字颜色修复
- 修复了深色主题下文字颜色看不清的问题
- 更新了警告按钮在深色主题下的颜色，从 `#1a1410` 改为 `#0f0e0c`，提高对比度
- 修复了蓝色徽章在深色主题下的颜色，背景使用半透明蓝色，文字使用浅蓝色 `#a5b4fc`
- 修复了PhobiaView.vue中的蓝绿色文本，在深色主题下使用更浅的 `#5acce6`
- 为TestsView.vue中的恐惧症测试添加了深色主题专用颜色 `colorDark`
- 添加了通用规则，自动提高深色主题下深色文字的对比度
- 解决了多个可能在深色主题下看不清的深色（如 `#9d0208`, `#3a0ca3`, `#6d4c41`, `#bc6c25`）

## CSS语法错误修复
- 修复了 animations.css 中的语法错误警告
- 原始错误：`[esbuild css minify] ▲ [WARNING] Expected identifier but found "[" [css-syntax-error]`
- 问题出现在第251行：`@media (prefers-reduced-motion: reduce), [data-motion="reduced"] {`，语法不正确
- 修复方案：将媒体查询和属性选择器分开处理
- 创建独立的 `@media (prefers-reduced-motion: reduce)` 块
- 创建独立的 `[data-motion="reduced"]` 属性选择器块
- 构建结果：无警告，成功构建

## 2025年份更新和深色文字修复
### 2025年份更新为2026
- 修复了所有文档和页面中的2025年份，统一更新为2026
- 修复的文件包括：
  - `docs/cpp-guide.md`: 更新日期从2025-12-28改为2026-12-28
  - `src/views/BlogView.vue`: 文章日期从2025-12-24改为2026-12-24
  - `src/views/blog/CPPGuideView.vue`: 日期从2025-12-24改为2026-12-24
  - `src/App.vue`: 页脚版权从© 2025改为© 2026
  - `project/english-listen.md`: 所有版本日期更新为2026年（v2.3.2, v2.3.1, v2.3.0）

### 测试页面按钮深色文字修复
- 修复了测试页面中深色按钮在深色主题下文字看不清的问题
- 扩展了base.css中的深色主题修复规则，新增对以下按钮的支持：
  - 双相情感障碍测试（bipolar）: `#7209b7` (紫色)
  - 赌博障碍测试（gambling）: `#6d4c41` (棕色)
  - 躯体症状障碍测试（somatic）: `#bc6c25` (深橙色)
- 这些按钮在深色主题下将使用主色（`--color-primary`）替代原来的深色

### 紧急求助页面自救方法深色文字修复
- 修复了EmergencyView.vue中自救方法部分在深色主题下文字颜色太深的问题
- 问题：`.accordion-body` 使用 `var(--color-text-secondary)`，在深色主题下太暗
- 修复方案：
  1. 在base.css中添加通用修复规则：`[data-theme="dark"] .accordion-body { color: var(--color-text-primary) !important; }`
  2. 在main.css中添加专门修复：`[data-theme="dark"] .accordion-body { color: var(--color-text-primary) !important; }`
- 确保自救方法中的所有文本在深色主题下都有足够的对比度

### base.css文件恢复和修复
- 恢复了原始的base.css文件（包含完整的炽炭主题设计系统）
- 在文件末尾添加了所有测试页面按钮的颜色定义（16个测试按钮）
- 添加了深色主题下深色按钮的修复规则（bipolar, ptsd, ocd, substance, gambling, somatic）
- 添加了紧急求助页面自救方法深色文字修复规则
- 修复后的base.css保留了原始的设计系统，包括：
  - 噪点纹理和玻璃态效果
  - 圆角和阴影系统
  - 所有CSS变量定义
  - 动画缓动和过渡效果
  - 按钮、卡片、输入框等基础样式
- 构建验证：成功构建，无警告错误