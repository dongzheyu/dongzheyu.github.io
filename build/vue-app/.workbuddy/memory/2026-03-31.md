# 2026-03-31

## 创建 English Listen WinUI 项目页面
- 探索了 `D:\c++\WinUI3\English-Listen-WinUI` 源码，了解了完整的技术架构（C# + C++ 混合开发，MVVM，WinUI 3）
- 创建了 VitePress 项目文档 `project/english-listen-winui.md`，包含项目介绍、技术栈、架构、功能等详细内容
- 更新了 `project/.vitepress/config.ts` 侧边栏，添加新项目条目
- 更新了 `project/index.md` 首页的 hero actions 和 features
- 更新了 `src/views/ProjectsView.vue`，在项目列表中添加 English Listen WinUI 卡片（color=#f48c06）
- 执行 `npm run project:build` 构建成功，生成了 `public/project/english-listen-winui.html`

## Vue 性能优化（动画卡顿修复）
- **App.vue**: 鼠标光点改用 requestAnimationFrame 节流，hover/ripple 事件改用事件委托（document mouseover/mouseout）替代逐元素绑定，onUnmounted 正确清理 rAF
- **base.css**: 阴影系统简化（移除多重阴影叠加和 inset），噪点纹理固定 opacity，主题切换过渡时间从 400ms 降至 200ms（移除 box-shadow 过渡），删除 card shimmer 扫光动画
- **animations.css**: 所有动画 transform 改用 translate3d/scale3d 强制 GPU 合成层，添加 prefers-reduced-motion 媒体查询支持，hover 动画 transition 缩短至 200ms，删除 rotate 变换
- **main.css**: heroGlow 动画从 translate+rotate 降级为仅 opacity 呼吸，删除 CTA slowRotate 旋转伪元素，卡片 hover 移除 rotate 微旋转，feature-icon/project-icon/value-icon hover 移除 rotate
- **HomeView.vue**: animate-on-scroll transition 从 700ms 缩短至 500ms，添加 will-change 提示 + visible 后自动清除

## 删除鼠标光点 + 添加手动减少动画开关
- **删除鼠标跟随光点**: 移除 App.vue 中 cursorDot ref、handleMouseMove（rAF节流）、handleLinkHover/Leave 事件委托及相关 onMounted/onUnmounted 事件绑定；移除模板中 `<div ref="cursorDot" class="cursor-dot">`；移除 base.css 中 .cursor-dot 全部样式（深浅主题 + hover 状态）
- **添加手动减少动画开关**: App.vue 新增 reducedMotion ref + toggleReducedMotion，通过 `data-motion="reduced"` 属性控制；localStorage 持久化（key: reduced-motion）；导航栏主题切换按钮左侧添加闪电图标按钮（bi-lightning-charge / bi-lightning-charge-fill）；animations.css 的 prefers-reduced-motion 媒体查询扩展为同时支持 `[data-motion="reduced"]`；main.css 新增 .motion-toggle-btn 样式
