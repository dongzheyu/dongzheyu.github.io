name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 设置 GITHUB_TOKEN 权限
permissions:
  contents: write  # 允许推送代码
  actions: read    # 允许读取工作流状态

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [ubuntu-24.04, windows-2022, macos-14]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          persist-credentials: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: build/vue-app/package-lock.json

      - name: Install dependencies
        run: |
          cd build/vue-app
          npm ci

      - name: Create .env.local
        run: |
          cd build/vue-app
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" > .env.local
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env.local
          echo "VITE_ADMIN_EMAIL=${{ secrets.VITE_ADMIN_EMAIL }}" >> .env.local

      - name: Build project
        run: |
          cd build/vue-app
          npm run build

      - name: Verify build output
        shell: bash
        run: |
          echo "Checking build output in root directory..."
          
          if [ -f "index.html" ]; then
            echo "✓ index.html found in root directory"
          else
            echo "✗ index.html not found in root directory"
            exit 1
          fi
          
          if [ -d "assets" ]; then
            echo "✓ assets directory found"
            echo "  Files in assets: $(ls assets | wc -l)"
          else
            echo "✗ assets directory not found"
            exit 1
          fi
          
          if [ -d "blog" ]; then
            echo "✓ blog directory found"
          else
            echo "✗ blog directory not found"
            exit 1
          fi
          
          if [ -d "project" ]; then
            echo "✓ project directory found"
          else
            echo "✗ project directory not found"
            exit 1
          fi
          
          if [ -d "SniShaperWeb" ]; then
            echo "✓ SniShaperWeb directory found"
            echo "  Files: $(ls SniShaperWeb)"
          else
            echo "⚠ SniShaperWeb directory not found (optional)"
          fi

      - name: Check file timestamps
        shell: bash
        run: |
          echo "Checking file modification times..."
          echo "index.html: $(stat -c '%y' index.html 2>/dev/null || stat -f '%Sm' index.html)"
          echo "assets directory: $(stat -c '%y' assets 2>/dev/null || stat -f '%Sm' assets)"

      - name: Commit and push changes
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && matrix.os == 'ubuntu-24.04'
        run: |
          git config --local user.email "youmao2023@outlook.com"
          git config --local user.name "dongle"
          git add -A
          git reset HEAD 404.html || true
          git diff --staged --quiet || git commit -m "ci: auto-build and deploy [skip ci]"
          git pull --rebase https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git main || true
          git push --force-with-lease https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        if: success()
        with:
          name: build-output-${{ matrix.os }}
          path: |
            index.html
            assets/
            blog/
            project/
            *.html
            *.css
            *.js
            *.png
            *.ico
            *.woff*
            *.ttf
            _redirects
            manifest.json
          retention-days: 7